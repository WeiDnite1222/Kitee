[project]
name = "Kitee"
version = "1.0"
description = "A custom Minecraft launcher (Forked from BakeLauncher)"
requires-python = ">=3.12, <3.13.0"
dependencies = [
    "pywebview==5.4", "tomli-w", "requests", "nuitka==2.7.10"
]
